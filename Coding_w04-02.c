#include <stdio.h>//ประกาศไลบรารีมาตรฐานสำหรับการใช้งานฟังก์ชัน printf

int main(){//ประกาศฟังก์ชัน main
    int a = 10;//ปรกาศตัวแปร a และกำหนดค่าเริ่มต้นเป็น 10
    printf("a = %d",a);//แสดงค่าของตัวแปร a บนหน้าจอ
    return 0;//ส่งค่ากลับ 0 เพื่อบ่งบอกว่าการทำงานสำเร็จ


}